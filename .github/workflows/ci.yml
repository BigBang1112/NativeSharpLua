name: CI

on:
  push:
    branches: [ "main", "dev" ]
    paths: [ ".github/workflows/*.yml", "NativeSharpLua/**", "NativeSharpLua.Tests/**", "NativeSharpLua.sln" ]
  pull_request:
    branches: [ "main", "dev" ]
    paths: [ ".github/workflows/*.yml", "NativeSharpLua/**", "NativeSharpLua.Tests/**", "NativeSharpLua.sln" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-test:
    name: Build lz4 ${{ vars.LUA_VERSION }} package + test
    uses: ./.github/workflows/build-package.yml
    with:
      version: ${{ vars.LZ4_VERSION }}
      runs-on: "['ubuntu-latest', 'windows-latest']"
